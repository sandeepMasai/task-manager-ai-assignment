# Optional. Leave unset for local dev (Vite proxy to http://localhost:5000).
# Set to your backend URL when building for production (e.g. Render backend).
VITE_API_URL=https://task-manager-ai-assignment-6lqr.onrender.com
